<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit User</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex h-screen">

  <form action="/update/<%= user._id %>" method="POST" class="bg-white p-10 rounded-xl shadow-lg flex flex-col gap-4 w-96">
    <h1 class="text-3xl font-bold text-center mb-2">Edit User</h1>

    <input type="text" name="name" value="<%= user.name %>" class="border border-gray-400 p-2 rounded" required>
    <input type="email" name="email" value="<%= user.email %>" class="border border-gray-400 p-2 rounded" required>
    <input type="text" name="image" value="<%= user.image %>" class="border border-gray-400 p-2 rounded" required>

    <button type="submit" class="bg-blue-600 text-white py-2 rounded hover:bg-blue-700">Update User</button>
  </form>

</body>
</html>
